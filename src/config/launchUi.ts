/**
 * Pre-launch: hide pieces that are not finished yet (opening hours block, Instagram).
 * Flip each to `true` when ready for go-live.
 *
 * Find all gates:
 *   rg "LAUNCH_UI_OPENING_HOURS|LAUNCH_UI_INSTAGRAM"
 */
export const LAUNCH_UI_OPENING_HOURS = false;
export const LAUNCH_UI_INSTAGRAM = false;
